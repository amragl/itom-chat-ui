# ITOM Chat Frontend — Environment Variables
# Copy this file to .env.local and adjust values as needed.
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.

# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket URL for real-time communication
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws

# ---------------------------------------------------------------------------
# Authentication Mode
# ---------------------------------------------------------------------------
# AUTH_MODE controls the authentication strategy:
#   sso — (default) Full ServiceNow OAuth 2.0 flow. Requires valid
#          SERVICENOW_INSTANCE, CLIENT_ID, and CLIENT_SECRET below.
#   dev — Development bypass. Skips OAuth entirely, injects a static dev
#          user into the session. No ServiceNow instance required.
AUTH_MODE=sso
NEXT_PUBLIC_AUTH_MODE=sso

# ---------------------------------------------------------------------------
# Authentication — ServiceNow OAuth 2.0
# ---------------------------------------------------------------------------

# ServiceNow instance URL (no trailing slash)
# Example: https://dev12345.service-now.com
SERVICENOW_INSTANCE=https://your-instance.service-now.com

# OAuth 2.0 client ID from ServiceNow Application Registry
SERVICENOW_CLIENT_ID=your-client-id

# OAuth 2.0 client secret from ServiceNow Application Registry
SERVICENOW_CLIENT_SECRET=your-client-secret

# Auth.js secret for encrypting JWTs and session cookies.
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-here

# Auth.js base URL (canonical URL of the application)
NEXTAUTH_URL=http://localhost:3000
